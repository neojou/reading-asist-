<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# ✍️ Writer — 網誌草稿

> **整合來源**：`borrow_checker.md`  + Verifier 查核  + Critic 論述修正  + Synthesizer 跨域洞見[^1][^2][^3][^4][^5][^6][^7][^8][^9]
> **風格**：面向有基礎的開發者，敘事流暢，兼顧技術深度與可讀性

***

# 為什麼 Rust 能在 Compile Time 抓出 Data Race，而 Kotlin 做不到？

*一個關於型別系統哲學的故事*

***

記憶體，是被所有執行緒共享的公地。

當多個執行緒同時爭奪這塊公地——一邊讀、一邊寫、誰也不等誰——就會發生 **Data Race**：程式不崩潰，但資料已悄悄損壞；測試可能通過，但在生產環境的某個深夜，它突然出現了。

這類 bug 惡名昭彰，難以重現、難以定位，是並發程式設計的頭號惡夢。

Rust 的答案令人驚訝：**讓這種 bug 從一開始就無法被編譯出來**。

不是執行期偵測，不是 linter 警告——是**編譯失敗**。

Kotlin 則做不到這一點。但這不是 Kotlin 工程師不夠努力，而是兩種語言在設計之初，就走上了完全不同的路。

***

## 什麼是 Data Race？

在深入之前，先對齊定義。Data race 的發生需要同時滿足三個條件 ：[^3]

1. **兩個以上的執行緒**同時存取**同一塊記憶體**
2. 其中**至少一個是寫入**操作
3. **沒有任何同步機制**（mutex、atomic 等）保護

三個條件缺一不可。這意味著：只要能在語言層面**從結構上保證這三個條件不會同時成立**，data race 就不可能發生——Rust 正是這樣做的。

***

## Rust 的答案：三條鐵律

Rust 的編譯器中有一個叫做 **Borrow Checker** 的靜態分析模組，它在程式碼被編譯的那一刻，強制執行三條規則 ：[^2][^1]


| 規則 | 內容 | 消滅的問題 |
| :-- | :-- | :-- |
| **Rule 1：唯一可變** | 同一作用域內，同一資料只能有**一個** `&mut` | 防止多處同時寫入 |
| **Rule 2：共享不可變** | 有多個 `&` 時，不得同時存在 `&mut` | 確保讀取期間沒有人在寫 |
| **Rule 3：Lifetime 界定** | 所有 reference 的存活時間不得超過資料的擁有者 | 消滅 use-after-free |

Rule 1 + Rule 2 合起來，從邏輯上封死了 data race 的發生條件：**「有人在寫」和「有人在讀或寫」這兩件事，在 safe Rust 中永遠不會同時成立**。

來看一個具體例子 ：[^1]

```rust
fn main() {
    let mut data = vec![1, 2, 3];

    let r1 = &data;       // ✅ immutable borrow
    let r2 = &data;       // ✅ 多個 immutable borrow 可以共存
    // let w = &mut data; // ❌ 編譯錯誤！r1, r2 還活著，不能同時有 &mut

    println!("{:?} {:?}", r1, r2);
    // r1, r2 在這裡結束生命週期

    let w = &mut data;    // ✅ 現在可以了，因為 r1, r2 已結束
}
```

這不是執行期的檢查，是 `rustc` 的靜態分析，**沒有任何 runtime overhead** 。違規的程式碼在你按下 build 的那一刻就被拒絕，還會附上明確的錯誤訊息告訴你衝突在哪裡。[^3]

***

## 這不是工程師的魔法，而是數學

很多人以為 borrow checker 是 Rust 工程師發明的聰明黑魔法，但它其實有嚴格的數學理論根基。

Rust 的 ownership 模型本質上是 **Affine Type System**（仿射型別系統），源自邏輯學家 Jean-Yves Girard 在 1987 年提出的 **Linear Logic**（線性邏輯）。這個理論研究的核心問題是：**資源（resource）應該被使用幾次？**[^7][^9]


| 型別系統 | 規則 | 代表語言 |
| :-- | :-- | :-- |
| **Linear Types** | 資源必須使用**恰好一次** | Haskell（GHC Linear Types） |
| **Affine Types** | 資源最多使用**一次**（可以丟棄） | **Rust** |
| 無限制引用 | 資源可使用**任意多次** | Kotlin、Java、Python |

Rust 選擇 Affine 而非嚴格 Linear，是為了允許 `drop`（丟棄值而不使用），讓語言保持實用性 。這個數學結構從根本上保證了：**在型別系統層面，一個 mutable reference 不可能有分身**，於是 data race 的前提條件在型別層就被消滅了。[^8]

***

## Kotlin 為何做不到？

這裡要先給一個公道的說法：**不是 Kotlin 不努力，而是 GC 設計從根本上決定了它的上限** 。[^8][^1]

### 問題根源：Aliasing

Kotlin 依賴 JVM 的 Garbage Collector 管理記憶體。GC 的存在前提是：**多個 reference 可以同時指向同一個物件**（aliasing），讓 GC 追蹤誰還在用它。

但這個前提，恰好是 affine type 系統的對立面。

```kotlin
// Kotlin：完全合法的程式碼，但潛藏 data race
val data = mutableListOf(1, 2, 3)
val alias1 = data  // ← 指向同一個 list
val alias2 = data  // ← 也指向同一個 list

// Thread A 透過 alias1 寫入，Thread B 透過 alias2 讀取
// Kotlin 編譯器不知道 alias1 和 alias2 是同一個物件
// → data race，但編譯成功 💥
```

Kotlin 的編譯器看到 `alias1` 和 `alias2`，**無從得知它們指向同一塊記憶體**。沒有 aliasing 追蹤，就無法做出「有人在寫，所以現在不能讀」的靜態保證 。[^4][^10]

### Kotlin 的應對方式

這不代表 Kotlin 對 data race 束手無策，只是它的防線在不同的地方 ：[^11][^4]

- **開發者手動加鎖**：使用 `Mutex`、`Atomic`、`limitedParallelism`
- **執行期工具**：Google 在生產環境使用 ThreadSanitizer（TSAN）在 test runtime 偵測 race condition
- **靜態分析工具**：Google 已開發基於 heuristics 的 Kotlin static analysis，可在 compile time 偵測**部分** racy pattern

關鍵差異在於：Kotlin 的這些防護都依賴**開發者紀律**或**外部工具**，而不是語言本身的強制保證。**你寫了有 race 的程式碼，Kotlin 編譯器不會攔你**。

***

## 一個必要的誠實補充：`unsafe` 的存在

說 Rust 保證無 data race，需要加上一個重要的限定語：**這個保證只在 safe Rust 的範圍內成立** 。[^5][^12]

Rust 提供 `unsafe` block，允許開發者繞過 borrow checker，直接操作裸指標、呼叫 FFI、使用 `UnsafeCell`。在 `unsafe` 中，data race 是可能發生的——Rust 官方 Reference 明確將其列為 Undefined Behavior。

```rust
// 在 unsafe 中，開發者自己負責安全性
unsafe {
    // 這裡的記憶體操作不受 borrow checker 保護
    *raw_ptr = 42;
}
```

好消息是：`unsafe` 在現實的 Rust codebase 中使用率極低，通常集中在底層函式庫，而非業務邏輯層 。大多數應用層程式碼完全活在 safe Rust 的保護罩之下。[^12]

***

## 更大的設計哲學

這整件事背後，有一個值得深思的設計哲學差異：

> **Rust 選擇讓非法狀態在型別系統中無法被表達。**
> **Kotlin 選擇讓非法狀態在執行期被偵測或由開發者預防。**

沒有絕對的對錯，只有不同的取捨。Rust 的代價是更陡的學習曲線，以及 borrow checker 偶爾拒絕合法程式碼的摩擦感 。Kotlin 的代價是並發安全永遠是「開發者責任」而非「語言責任」。[^6]

用一個簡單的類比來說：Rust 像是把「禁止同時讀寫同一份文件」寫進了**辦公室的建築結構**——物理上就是做不到；Kotlin 像是貼了一張**告示牌**——規則在那裡，但沒有東西阻止你無視它。

***

## 語言光譜

這不是非黑即白的問題。各語言在「並發安全強制程度」上，形成一個連續光譜 ：[^13]

```
強制程度（低） ←————————————————————→ 強制程度（高）
  C/C++    Python   Kotlin/Java   Go   Rust (safe)   SPARK/Ada
  完全自理    GIL鎖      手動加鎖       channel    編譯器保證      形式驗證
```

Rust 在主流語言中，站在最靠近「編譯器強制保證」的那一端，但它不是終點——SPARK 和 Idris 等語言用形式驗證走得更遠，代價是更高的使用門檻。

***

## 一句話總結

**Rust 能在 compile time 抓出 data race，是因為它的型別系統（Affine Type System）從數學層面保證「可變 reference 不能有分身」，讓 data race 的前提條件在靜態分析時就不可能成立。Kotlin 做不到，是因為 GC 的設計允許 aliasing 存在，而沒有 aliasing 追蹤，語言就無法在編譯期做出「無 data race」的完整保證——不是 Kotlin 不努力，是 GC 決定了它的型別系統上限。**

***

*參考資料：[Rust Borrow Checker Explained](https://trippy.dev/blogs/programming/rusts-borrow-checker-and-its-role-in-preventing-data-races) · [Introducing the Borrow Checker — LogRocket](https://blog.logrocket.com/introducing-rust-borrow-checker/) · [Prevent Race Conditions in Coroutines — Dave Leeds](https://typealias.com/articles/prevent-race-conditions-in-coroutines/) · [Behavior Considered Undefined — Rust Reference](https://doc.rust-lang.org/reference/behavior-considered-undefined.html) · [From Linear Types to Rust — YouTube](https://www.youtube.com/watch?v=BWuozpQ_Of8)*

***

> **Writer 備註**：本草稿已整合 Verifier 的兩項查核修正（safe Rust caveat、Kotlin static analysis 的有限能力）、Critic 的三項論述精修（unsafe block、aliasing 的精確說法、borrow checker 摩擦感），以及 Synthesizer 的跨域框架（Affine Type Theory、公地悲劇、語言光譜）。如需調整語氣、長度或增加特定章節，請指示 `Writer:` 繼續修改，或呼叫 `Coordinator:` 產出最終報告。[^5][^6][^7][^11][^8]

<div align="center">⁂</div>

[^1]: borrow_checker.md

[^2]: https://trippy.dev/blogs/programming/rusts-borrow-checker-and-its-role-in-preventing-data-races

[^3]: https://blog.logrocket.com/introducing-rust-borrow-checker/

[^4]: https://typealias.com/articles/prevent-race-conditions-in-coroutines/

[^5]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html

[^6]: https://news.ycombinator.com/item?id=42485536

[^7]: https://corecursive.com/016-moves-and-borrowing-in-rust-with-jim-blandy/

[^8]: https://timhwang21.gitbook.io/index/blog/2020

[^9]: https://www.youtube.com/watch?v=BWuozpQ_Of8

[^10]: https://www.linkedin.com/posts/daveleeds_how-to-prevent-race-conditions-in-kotlin-activity-7398743882827706368-2ksW

[^11]: https://www.youtube.com/watch?v=SVlY7Mca1xg

[^12]: https://rustmagazine.org/issue-3/understand-unsafe-rust

[^13]: https://uplatz.com/blog/memory-safety-in-systems-programming-a-comparative-analysis-of-modern-alternatives-to-c-c/

